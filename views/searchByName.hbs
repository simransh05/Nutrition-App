<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/navbar.css">
    <link rel="stylesheet" href="/styles/nameSearch.css">
</head>
<body class="body">
    {{>navbar user=user}}

    <div class="page-wrapper">
        <div class="search-name-container">
            <h2 class="search-name-heading">Search Food By Name</h2>

            <form class="search-name-form" action="/searchName" method="post">
                <label for="name" class="form-label">Enter Name of Food Item:</label>
                <input class="form-input" type="text" name="name" placeholder="Enter Name" required />
                <button class="form-button" type="submit">Search</button>
            </form>

            <h3 class="history-heading">Search History</h3>
            <ul class="history-list">
                {{#each history1}}
                <li class="history-item">
                    <span class="history-text">{{this.name}}</span>
                    <form class="delete-form" action="/delete" method="POST" style="display:inline;">
                        <input type="hidden" name="historyId" value="{{this.id}}" />
                        <button class="delete-button" type="submit">❌</button>
                    </form>
                </li>
                {{/each}}
            </ul>
        </div>
    </div>
</body>
</html>
